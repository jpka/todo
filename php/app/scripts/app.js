(function() {

  define(["jquery", "lodash", "backbone"], function($, _, Backbone) {
    var app;
    app = {
      url: "http://localhost/dev/todos/php/api"
    };
    return app;
  });

}).call(this);
